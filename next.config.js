/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['gamek.mediacdn.vn', 'cdn.pixabay.com', 'genk.mediacdn.vn'],
  },
};

module.exports = nextConfig;
